const body = document.body;
let title = document.createElement("h1");
let tempText = document.createElement("p");
let form = document.getElementById("form");
let time = document.getElementById("time");
let minAff = document.getElementById("min");
let secAff = document.getElementById("sec");
let stop = document.getElementById("stop");
let start = document.getElementById("start");
let nbInt = 0;
let timebis = document.getElementById("timebis");

let request = 0;
stop.disabled = true;
body.append(title,tempText);

title.textContent = "Timer POMODORO";

let secondes = 0;
let min = 0;

console.log(timebis);
console.log(timebis.value);

function mafonction(){
    request--;
    secAff.textContent= request%60 < 10? "0" + request%60:request%60;
    minAff.textContent= Math.trunc(request/60)<10 ? "0"+Math.trunc(request/60)+" :":Math.trunc(request/60) + " :";

    if(request==0){
        clearInterval(nbInt);
    }
}

function Start(){
    request = time.value*60;
    console.log(stop);
    stop.disabled = false;
    let dmr = setInterval(mafonction, 1000);
    start.disabled = true;
    nbInt++;
}
function StopButton(){
    clearInterval(1);
    stop.disabled = true;
    start.disabled = false;
}


form.addEventListener("submit", (evt) => {
    evt.preventDefault();
    Start();
})